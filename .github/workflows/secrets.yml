name: Secrets
on: push
jobs:
  job1:
    runs-on: windows-latest
    steps:
      - name: Test
        run: |
          echo ${REACT_APP_TOKEN}
        env:
          REACT_APP_TOKEN: ${{ secrets.REACT_APP_TOKEN }}